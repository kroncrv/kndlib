{
  "name": "kndlib",
  "version": "0.1.0",
  "license": "MIT",
  "main": "src/index.js",
  "repository": "gitlab:_pointer/kndlib",
  "scripts": {
    "build": "npm run clean && npm run build:javascript",
    "build:javascript": "npx babel src -d . -D --presets=env",
    "clean": "rm -rf lib vue",
    "watch": "npx babel -w src -d . -D --presets=env"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  },
  "dependencies": {
    "archieml": "^0.4.2",
    "lodash": "^4.17.15",
    "node-fetch": "^2.6.0",
    "read-excel-file": "^4.0.2"
  }
}
